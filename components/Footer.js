import { Layout } from "antd";
const { Footer } = Layout;

const FooterComponent = () => {
  return (
    <Footer style={{ textAlign: "center" }}>NEXT WITH GRAPHQL POC ©2021</Footer>
  );
};

export default FooterComponent;
